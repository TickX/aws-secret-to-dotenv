{
  "name": "aws-secret-to-dotenv",
  "version": "1.0.0",
  "description": "A GitHub action that appends an AWS Secret value(s) to a dotenv file",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "npx ncc build src/main.ts -o dist",
    "test": "npx jest"
  },
  "dependencies": {
    "@actions/core": "^1.2.6",
    "aws-sdk": "^2.814.0",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "@types/jest": "^25.1.1",
    "@types/node": "^13.7.0",
    "@types/sinon": "^7.5.1",
    "@vercel/ncc": "^0.25.1",
    "jest": "^25.1.0",
    "sinon": "^8.1.1",
    "ts-jest": "^25.2.0",
    "typescript": "^3.7.5"
  }
}
